{
  ".verification-assets/cdk.out/verify/filters/ShinBucketDeploymentFiltersDemo.assets.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged.",
  ".verification-assets/cdk.out/verify/filters/ShinBucketDeploymentFiltersDemo.template.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged.",
  ".verification-assets/cdk.out/verify/filters/manifest.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged.",
  ".verification-assets/cdk.out/verify/filters/tree.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/ShinBucketDeploymentStaleObjectCleanupDemo.assets.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/ShinBucketDeploymentStaleObjectCleanupDemo.template.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/manifest.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/tree.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/ShinBucketDeploymentStaleObjectCleanupDemo.assets.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/ShinBucketDeploymentStaleObjectCleanupDemo.template.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/manifest.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/tree.json": "Replace directory-packed listing-key fixtures with a deterministic prebuilt ZIP file so CR/LF names reach S3 unchanged; scenario behavior and expected keys are unchanged."
}
