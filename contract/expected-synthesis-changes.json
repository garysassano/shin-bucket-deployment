{
  ".verification-assets/cdk.out/verify/filters/ShinBucketDeploymentFiltersDemo.assets.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior.",
  ".verification-assets/cdk.out/verify/filters/ShinBucketDeploymentFiltersDemo.template.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior.",
  ".verification-assets/cdk.out/verify/filters/manifest.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior.",
  ".verification-assets/cdk.out/verify/filters/tree.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/ShinBucketDeploymentStaleObjectCleanupDemo.assets.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/ShinBucketDeploymentStaleObjectCleanupDemo.template.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/manifest.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/tree.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/ShinBucketDeploymentStaleObjectCleanupDemo.assets.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/ShinBucketDeploymentStaleObjectCleanupDemo.template.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/manifest.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/tree.json": "Use Shin onDelete cleanup for the owned listing-key fixture prefix instead of CDK bucket auto-delete, preserving exact CR/LF keys and Create/Update behavior."
}
