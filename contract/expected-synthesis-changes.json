{
  ".verification-assets/cdk.out/verify/child-parent-cleanup-initial/ShinBucketDeploymentChildParentCleanupDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-initial/ShinBucketDeploymentChildParentCleanupDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-initial/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-initial/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-updated/ShinBucketDeploymentChildParentCleanupDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-updated/ShinBucketDeploymentChildParentCleanupDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-updated/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-updated/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-retention-initial/ShinBucketDeploymentChildParentRetentionDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-retention-initial/ShinBucketDeploymentChildParentRetentionDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-retention-initial/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-retention-initial/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-retention-updated/ShinBucketDeploymentChildParentRetentionDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-retention-updated/ShinBucketDeploymentChildParentRetentionDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-retention-updated/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/child-parent-retention-updated/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-async-initial/ShinBucketDeploymentCloudFrontAsyncDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-async-initial/ShinBucketDeploymentCloudFrontAsyncDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-async-initial/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-async-initial/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-async-updated/ShinBucketDeploymentCloudFrontAsyncDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-async-updated/ShinBucketDeploymentCloudFrontAsyncDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-async-updated/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-async-updated/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-initial/ShinBucketDeploymentCloudFrontSyncDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-initial/ShinBucketDeploymentCloudFrontSyncDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-initial/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-initial/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-updated/ShinBucketDeploymentCloudFrontSyncDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-updated/ShinBucketDeploymentCloudFrontSyncDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-updated/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-updated/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-initial/ShinBucketDeploymentCoTenantSafetyDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-initial/ShinBucketDeploymentCoTenantSafetyDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-initial/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-initial/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-updated/ShinBucketDeploymentCoTenantSafetyDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-updated/ShinBucketDeploymentCoTenantSafetyDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-updated/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-updated/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-initial/ShinBucketDeploymentCrossBucketChangeDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-initial/ShinBucketDeploymentCrossBucketChangeDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-initial/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-initial/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-updated/ShinBucketDeploymentCrossBucketChangeDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-updated/ShinBucketDeploymentCrossBucketChangeDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-updated/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-updated/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/default-retention-initial/ShinBucketDeploymentDefaultRetentionDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/default-retention-initial/ShinBucketDeploymentDefaultRetentionDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/default-retention-initial/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/default-retention-initial/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/default-retention-updated/ShinBucketDeploymentDefaultRetentionDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/default-retention-updated/ShinBucketDeploymentDefaultRetentionDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/default-retention-updated/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/default-retention-updated/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/external-zips/ShinBucketDeploymentExternalZipsDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/external-zips/ShinBucketDeploymentExternalZipsDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/external-zips/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/external-zips/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/extract-false/ShinBucketDeploymentExtractFalseDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/extract-false/ShinBucketDeploymentExtractFalseDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/extract-false/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/extract-false/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/filters/ShinBucketDeploymentFiltersDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/filters/ShinBucketDeploymentFiltersDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/filters/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/filters/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/handler-isolation/ShinBucketDeploymentHandlerIsolationDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/handler-isolation/ShinBucketDeploymentHandlerIsolationDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/handler-isolation/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/handler-isolation/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/large-archive/ShinBucketDeploymentLargeArchiveDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/large-archive/ShinBucketDeploymentLargeArchiveDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/large-archive/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/large-archive/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/marker-replacement/ShinBucketDeploymentMarkerReplacementDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/marker-replacement/ShinBucketDeploymentMarkerReplacementDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/marker-replacement/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/marker-replacement/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/object-deletion-initial/ShinBucketDeploymentObjectDeletionDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/object-deletion-initial/ShinBucketDeploymentObjectDeletionDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/object-deletion-initial/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/object-deletion-initial/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/object-deletion-updated/ShinBucketDeploymentObjectDeletionDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/object-deletion-updated/ShinBucketDeploymentObjectDeletionDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/object-deletion-updated/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/object-deletion-updated/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/root-prefix/ShinBucketDeploymentRootPrefixDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/root-prefix/ShinBucketDeploymentRootPrefixDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/root-prefix/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/root-prefix/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/simple/ShinBucketDeploymentSimpleDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/simple/ShinBucketDeploymentSimpleDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/simple/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/simple/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/source-overwrite-order/ShinBucketDeploymentSourceOverwriteOrderDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/source-overwrite-order/ShinBucketDeploymentSourceOverwriteOrderDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/source-overwrite-order/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/source-overwrite-order/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/ShinBucketDeploymentStaleObjectCleanupDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/ShinBucketDeploymentStaleObjectCleanupDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/ShinBucketDeploymentStaleObjectCleanupDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/ShinBucketDeploymentStaleObjectCleanupDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-retention-initial/ShinBucketDeploymentStaleObjectRetentionDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-retention-initial/ShinBucketDeploymentStaleObjectRetentionDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-retention-initial/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-retention-initial/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-retention-updated/ShinBucketDeploymentStaleObjectRetentionDemo.assets.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-retention-updated/ShinBucketDeploymentStaleObjectRetentionDemo.template.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-retention-updated/manifest.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  ".verification-assets/cdk.out/verify/stale-object-retention-updated/tree.json": "Provider Lambda MemorySize default 1024 -> 2048 MiB and transfer.maxConcurrency default 32 -> 64 (T-1); every synthesized assembly that deploys the provider carries the new values.",
  "declaration defaults.d.ts": "T-1: the exported DEFAULT_PROVIDER_LAMBDA_MEMORY_SIZE_MIB and DEFAULT_TRANSFER_MAX_CONCURRENCY literal types change from 1024/32 to 2048/64.",
  "declaration shin-bucket-deployment.d.ts": "T-1: the @default annotations and the memorySize doc comment change to match the new defaults and to record the measured evidence."
}
