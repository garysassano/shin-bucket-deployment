{
  ".benchmark-assets/cdk.out/benchmark/assets-shin-tiny-many-1024-32/ShinBucketDeploymentBenchmarkAssetsDemo-shin-tiny-many-1024-32.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".benchmark-assets/cdk.out/benchmark/assets-shin-tiny-many-1024-32/ShinBucketDeploymentBenchmarkAssetsDemo-shin-tiny-many-1024-32.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".benchmark-assets/cdk.out/benchmark/assets-shin-tiny-many-1024-32/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".benchmark-assets/cdk.out/benchmark/assets-shin-tiny-many-1024-32/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-initial/ShinBucketDeploymentChildParentCleanupDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-initial/ShinBucketDeploymentChildParentCleanupDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-initial/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-initial/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-updated/ShinBucketDeploymentChildParentCleanupDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-updated/ShinBucketDeploymentChildParentCleanupDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-updated/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-cleanup-updated/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-retention-initial/ShinBucketDeploymentChildParentRetentionDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-retention-initial/ShinBucketDeploymentChildParentRetentionDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-retention-initial/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-retention-initial/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-retention-updated/ShinBucketDeploymentChildParentRetentionDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-retention-updated/ShinBucketDeploymentChildParentRetentionDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-retention-updated/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/child-parent-retention-updated/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-async-initial/ShinBucketDeploymentCloudFrontAsyncDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-async-initial/ShinBucketDeploymentCloudFrontAsyncDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-async-initial/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-async-initial/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-async-updated/ShinBucketDeploymentCloudFrontAsyncDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-async-updated/ShinBucketDeploymentCloudFrontAsyncDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-async-updated/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-async-updated/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-initial/ShinBucketDeploymentCloudFrontSyncDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-initial/ShinBucketDeploymentCloudFrontSyncDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-initial/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-initial/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-updated/ShinBucketDeploymentCloudFrontSyncDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-updated/ShinBucketDeploymentCloudFrontSyncDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-updated/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cloudfront-sync-updated/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-initial/ShinBucketDeploymentCoTenantSafetyDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-initial/ShinBucketDeploymentCoTenantSafetyDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-initial/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-initial/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-updated/ShinBucketDeploymentCoTenantSafetyDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-updated/ShinBucketDeploymentCoTenantSafetyDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-updated/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/co-tenant-protection-updated/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-initial/ShinBucketDeploymentCrossBucketChangeDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-initial/ShinBucketDeploymentCrossBucketChangeDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-initial/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-initial/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-updated/ShinBucketDeploymentCrossBucketChangeDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-updated/ShinBucketDeploymentCrossBucketChangeDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-updated/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/cross-bucket-change-updated/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/default-retention-initial/ShinBucketDeploymentDefaultRetentionDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/default-retention-initial/ShinBucketDeploymentDefaultRetentionDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/default-retention-initial/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/default-retention-initial/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/default-retention-updated/ShinBucketDeploymentDefaultRetentionDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/default-retention-updated/ShinBucketDeploymentDefaultRetentionDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/default-retention-updated/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/default-retention-updated/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/external-zips/ShinBucketDeploymentExternalZipsDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/external-zips/ShinBucketDeploymentExternalZipsDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/external-zips/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/external-zips/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/extract-false/ShinBucketDeploymentExtractFalseDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/extract-false/ShinBucketDeploymentExtractFalseDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/extract-false/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/extract-false/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/filters/ShinBucketDeploymentFiltersDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/filters/ShinBucketDeploymentFiltersDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/filters/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/filters/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/handler-isolation/ShinBucketDeploymentHandlerIsolationDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/handler-isolation/ShinBucketDeploymentHandlerIsolationDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/handler-isolation/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/handler-isolation/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/large-archive/ShinBucketDeploymentLargeArchiveDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/large-archive/ShinBucketDeploymentLargeArchiveDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/large-archive/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/large-archive/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/marker-replacement/ShinBucketDeploymentMarkerReplacementDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/marker-replacement/ShinBucketDeploymentMarkerReplacementDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/marker-replacement/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/marker-replacement/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/object-deletion-initial/ShinBucketDeploymentObjectDeletionDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/object-deletion-initial/ShinBucketDeploymentObjectDeletionDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/object-deletion-initial/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/object-deletion-initial/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/object-deletion-updated/ShinBucketDeploymentObjectDeletionDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/object-deletion-updated/ShinBucketDeploymentObjectDeletionDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/object-deletion-updated/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/object-deletion-updated/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/replacement-safety-initial/ShinBucketDeploymentReplacementSafetyDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/replacement-safety-initial/ShinBucketDeploymentReplacementSafetyDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/replacement-safety-initial/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/replacement-safety-initial/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/replacement-safety-updated/ShinBucketDeploymentReplacementSafetyDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/replacement-safety-updated/ShinBucketDeploymentReplacementSafetyDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/replacement-safety-updated/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/replacement-safety-updated/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/root-prefix/ShinBucketDeploymentRootPrefixDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/root-prefix/ShinBucketDeploymentRootPrefixDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/root-prefix/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/root-prefix/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/simple/ShinBucketDeploymentSimpleDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/simple/ShinBucketDeploymentSimpleDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/simple/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/simple/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/source-overwrite-order/ShinBucketDeploymentSourceOverwriteOrderDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/source-overwrite-order/ShinBucketDeploymentSourceOverwriteOrderDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/source-overwrite-order/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/source-overwrite-order/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/ShinBucketDeploymentStaleObjectCleanupDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/ShinBucketDeploymentStaleObjectCleanupDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-initial/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/ShinBucketDeploymentStaleObjectCleanupDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/ShinBucketDeploymentStaleObjectCleanupDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-cleanup-updated/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-retention-initial/ShinBucketDeploymentStaleObjectRetentionDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-retention-initial/ShinBucketDeploymentStaleObjectRetentionDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-retention-initial/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-retention-initial/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-retention-updated/ShinBucketDeploymentStaleObjectRetentionDemo.assets.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-retention-updated/ShinBucketDeploymentStaleObjectRetentionDemo.template.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-retention-updated/manifest.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed.",
  ".verification-assets/cdk.out/verify/stale-object-retention-updated/tree.json": "0.12.0 version bump: the package version participates in the shared provider-handler identity, so every synthesized assembly's handler logical ID and its asset/template/manifest references change. No provider or construct logic changed."
}
