# Verification

This page is the current human-readable correctness snapshot for `ShinBucketDeployment`. Performance evidence and AWS CDK `BucketDeployment` comparisons remain separate in [benchmark](./benchmark.md). Verification is replaced rather than appended; runbooks and sanitization rules live in `.agents/skills/shin-verification/SKILL.md`.

> [!IMPORTANT]
> The task-drain-backstop candidate at `1bd6a5b` has complete deterministic local verification that spawned-task cleanup ends before the outer request-processing timeout can cancel that cleanup. Scheduler cleanup, ZIP body cleanup, and every source store still share one lazily started five-second budget, now followed by a one-second unwind guard before callback-only time. A healthy AWS deployment cannot force the monotonic deadline boundary or internal cancellation ordering, so no new AWS run was performed; the latest targeted AWS evidence remains the CopyObject create and tamper-repair run. The broader integration evidence remains recorded in the earlier component snapshots, and the expanded 33-phase matrix has not yet been rerun as one combined AWS pass. Raw AWS output and identifiers remain outside git.

## Current Snapshot

| Field | Value |
| --- | --- |
| Latest verification date | 2026-07-16 |
| Current verification baseline | Task-drain-backstop candidate at `1bd6a5b` |
| Current verification category | Deterministic local cancellation/deadline gates plus the latest targeted direct-copy AWS assertions |
| Latest current runs | `2026-07-16-local-task-drain-backstop` and `2026-07-16-aws-copy-retry-ownership` |
| AWS status for combined code | No new AWS deployment was run for the task-drain-backstop candidate because the boundary requires controlled monotonic time and internal task cancellation at the outer timeout edge. The latest targeted AWS run remains the direct-copy Create and tamper-repair Update, which produced and restored the exact ZIP, inferred content type, opaque reconciliation identity, and clean one-attempt CopyObject diagnostics. |
| Provider architecture exercised in the latest targeted AWS run | arm64 |
| Packaged provider architectures | arm64 and x86_64 archives rebuilt from the candidate source and structurally verified |
| Cleanup | The targeted stack was destroyed; its captured destination bucket and the stack were independently confirmed absent |
| Raw evidence | Raw CDK logs, CloudWatch output, responses, and identifiers were kept in scratch only and are not committed |
| Scenario runner | `pnpm verify list`, `pnpm verify synth`, `pnpm verify deploy`, or `pnpm verify destroy`; ordered update chains remain serial within concurrent groups |

## Current Coverage

| Priority | Area | Current Evidence | Status |
| --- | --- | --- | --- |
| P0 | Rust provider tests | The current candidate passed 168 tests with one credential-gated integration test ignored. Coverage includes callback retry/deadline accounting, diagnostics schema v3, deterministic bounded marker streaming, external ZIP layouts, fair aggregate source permits, exact owner-aware stale detection, child-to-parent cleanup planning, exact slash-run handling, indexed interval lookup, CopyObject retry ownership, serialized post-failure admission, work-deadline-aware retry/cooldown waits, one shared task-drain deadline, and separation from the outer request backstop. | Local pass |
| P0 | TypeScript and release-script tests | The current candidate passed 155 Vitest tests plus seven Node release tests, covering the exported `ValidationError` contract, installed CommonJS/ESM/TypeScript consumers, construct synthesis, benchmark telemetry, scenario ordering, lifecycle behavior, exact IAM boundaries, and shared/isolated handler topology. | Local pass |
| P0 | Build and static checks | The current candidate passed TypeScript, package, Biome, Rust, dependency, workflow, synthesis, benchmark-synthesis, and provider-build gates. Both packaged Lambda architectures were rebuilt and verified; no task-drain-backstop AWS deployment was needed for the deterministic cancellation boundary. | Local pass |
| P0 | Scenario synthesis | All 33 default verification phases across 23 stacks list and synthesize, including handler isolation, replacement, external-ZIP, owner-overlap, child-to-parent, cross-bucket, customer-managed KMS, AWS-managed KMS, and managed DSSE scenarios. | Local pass |
| P0 | Handler sharing and isolation | Omitted and explicit `shareHandler:true` synthesize identically. Shared identity includes package version and exact prebuilt archive digest; `shareHandler:false` creates stable deployment-scoped functions and generated roles whose policies do not accumulate grants from other deployments. Installed CommonJS, ESM, and TypeScript consumers exercised the public option. AWS template assertions found exactly three Shin provider functions, roles, and policies for two shared plus two isolated deployments, and all four exact object bodies were present. | Local and AWS pass |
| P0 | Marker replacement | Simultaneous leftmost-longest replacement is non-recursive and bounded across decompression chunks. AWS assertions checked plain, raw/escaped JSON, data, and YAML sources; telemetry recorded six planning and six upload passes using the declared two-pass strategy, with zero source GET errors/retries and zero destination PUT retries/throttles. | Local and AWS pass |
| P0 | Public object contract | The public props use a positive operational allowlist. Configurable object metadata, legacy `logRetention`, and unused `ephemeralStorageSize` are rejected; PUT/COPY infer `Content-Type` with a deterministic binary fallback. AWS assertions confirmed HTML, text, ZIP, and binary results. | Local and AWS pass |
| P0 | Encryption strategy | Default/AES256 buckets synthesize `sse-s3-etag`; KMS/DSSE variants synthesize `kms-sha256`. Imported, unknown, tokenized-algorithm, multi-rule, and ungrantable L1 customer-key shapes are rejected. | Local pass |
| P0 | KMS and DSSE writes | Customer KMS, AWS-managed S3 KMS, and managed DSSE uploads completed in AWS. Checksum-mode `HeadObject` independently confirmed exact content length, encryption mode, stored SHA-256, and `FULL_OBJECT` checksum type; object bodies also matched. | AWS pass |
| P0 | Managed-key authorization | Synthesis constrains `kms:Decrypt` and exact `kms:GenerateDataKey` access to the destination key. AWS-managed access is further limited to current account/Region KMS keys, `alias/aws/s3`, and regional S3 via-service use. Customer keys use CDK key grants, and unused describe, encrypt, and re-encrypt actions are absent. Real customer-managed and AWS-managed writes exercised both authorization paths. | Local and AWS pass |
| P0 | Content, filtering, and scale | AWS state assertions covered simple/root deployment, include/exclude filters, marker replacement and JSON escaping, duplicate-source overwrite order, `extract=false`, and the 24 MiB ranged-read object. | AWS pass |
| P0 | Direct-copy safety | Source `HeadObject` supplies an exact ETag and length before mutation. Copy requests carry the source ETag, a destination `If-None-Match` or `If-Match` guard when listing data permits it, one disabled-retry SDK attempt per provider attempt, and an opaque operation identity written atomically with replacement metadata. Deterministic wire tests cover embedded HTTP-200 errors, typed transient and permanent failures, 409 retry, 412/final-ambiguous reconciliation, and marker/length mismatch rejection. The targeted AWS Create and tamper-repair Update both produced byte-exact ZIP output and clean one-attempt diagnostics. | Local and AWS pass |
| P0 | Destination lifecycle | Ordered AWS chains proved stale deletion, stale retention when `onDeploy.deleteStaleObjects=false`, default previous-prefix retention, explicit `onChange.deleteObjects`, and terminal `onDelete.deleteObjects`. Direct assertions additionally proved generation-specific replacement ownership, root/co-tenant ownership protection, unauthorized child-to-parent retention despite default stale cleanup, authorized manifest-aware child cleanup with normal stale deletion disabled, and explicit cross-bucket cleanup while current destination deletion remained disabled. | AWS pass |
| P0 | Destination IAM | Current-destination synthesis grants exact `GetObject`, `PutObject`, and prefix-scoped `ListBucket`; it adds exact `DeleteObject` and ownership-tag reads only when current cleanup is enabled. Explicit `onChange.deleteObjects` grants the selected old bucket the broad list/delete/tag authority required to discover the old runtime prefix. The targeted AWS chain proved that old-bucket cleanup and new-bucket writes both succeed under this split. | Local and AWS pass |
| P0 | Invocation memory and destination planning | Combined local tests bound independent archives with one fair permit pool, prove cancellation wakes waiters, bound retained destination data to manifest keys plus one page, and distinguish exact included stale candidates from manifests, filters, and empty-prefix markers. The component AWS snapshot captured 18 summaries within the exact source cap; the combined AWS pass exercised the rebased provider across all lifecycle scenarios. | Local and AWS integration pass |
| P0 | CloudFront invalidation | Both distributions were primed with long-TTL cached content, redeployed with distinct updated tokens, and checked again. The synchronous path served updated content when the stack returned; the asynchronous path converged within the bounded poll. | AWS pass |
| P0 | Lost-response convergence | SSE-S3 uploads accept only exact length plus streamed MD5/single-part ETag without checksum mode or ACL reads. KMS uploads accept only exact length plus stored `FULL_OBJECT` SHA-256 with checksum mode. Direct copies accept only the exact opaque operation identity plus content length and perform reconciliation with SDK retries disabled. | Deterministic local pass; AWS fault injection not performed |
| P0 | Diagnostics and callback delivery | Schema v3 separates deployment work from callback delivery, includes callback latency in total duration, reports provider-level deletion SDK calls and inferred quiet-response outcomes without claiming wire attempts or mutation, and records catalog trust/fallback work. Direct copies emit a separate structured CopyObject diagnostics record without changing the schema-v3 logical copy totals. The targeted AWS Create and repair each emitted one record with one wire attempt and zero failures/retries. | Local and AWS pass |
| P0 | Transfer scheduling and retry ownership | Logical object work is bounded by `maxParallelTransfers` and continuously drained. Failure publication and task admission share one serialized gate, so an unrelated successful completion cannot reopen admission after a retained task has failed; the exact masking regression passed 100 consecutive runs. Write retry waits and shared throttle cooldowns never sleep to or past the work deadline: a rejected retry preserves the originating `InternalError` or `SlowDown`, and a pre-existing cooldown stops a new wire attempt with a throttle-specific error. Transfer-task cleanup and every ZIP source-body cleanup phase share one lazily started absolute drain deadline, capped at five seconds total; that deadline ends one second before the outer request backstop, and the callback reserve begins only after the backstop. Deterministic tests also prove that the first error or panic aborts and drains outstanding tasks and wakes source waiters. Ranged GET, destination PutObject, and destination CopyObject use one SDK attempt per provider attempt and do not replay permanent failures. Healthy AWS phases completed with schema-v3 scheduler, wire-attempt, body-replay, throttling, and reader-high-water telemetry. | Local and AWS pass |

## Latest Verification Runs

| Run | Category | Scope | Status | Evidence |
| --- | --- | --- | --- | --- |
| `2026-07-16-local-task-drain-backstop` | local | Child-task drain/backstop separation, shared cleanup budget, callback reserve, package consumers, benchmark synthesis, and all scenarios at `1bd6a5b` | Pass | 168 Rust tests passed with one AWS-only test ignored; paused-time tests proved that cleanup receives the full five-second budget when normal work ends, completes one second before the outer request backstop, reuses the same deadline across later cleanup phases, and remains capped for short invocations; 155 Vitest and seven Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all 33 phases across 23 stacks synthesized; both Lambda architectures were rebuilt and package-verified; benchmark synthesis passed. No AWS run was performed because a healthy deployment cannot deterministically force the internal cancellation ordering at the monotonic timeout edge. |
| `2026-07-16-local-shared-task-drain-budget` | local | Shared scheduler/source-body drain deadline, callback-reserve cap, package consumers, benchmark synthesis, and all scenarios at `b670d2a` | Pass | 167 Rust tests passed with one AWS-only test ignored; paused-time tests proved that a later cleanup phase receives only the unused portion of the original five-second budget and that a short invocation remains capped before the callback reserve; 155 Vitest and seven Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all 33 phases across 23 stacks synthesized; both Lambda architectures were rebuilt and package-verified; benchmark synthesis passed. No AWS run was performed because a healthy deployment cannot deterministically make a cleanup task consume a selected part of the drain budget. |
| `2026-07-16-local-deadline-aware-write-retries` | local | PutObject/CopyObject retry waits, shared throttle cooldowns, error preservation, package consumers, benchmark synthesis, and all scenarios at `3a16780` | Pass | 165 Rust tests passed with one AWS-only test ignored; deterministic paused-time tests proved rejected waits preserve `InternalError` and `SlowDown`, shared cooldowns stop new wire attempts, and waits that fit still retry and record their duration; 155 Vitest and seven Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all 33 phases across 23 stacks synthesized; both Lambda architectures were rebuilt and package-verified; benchmark synthesis passed. No AWS run was performed because a healthy deployment cannot deterministically inject the required throttle/deadline boundary. |
| `2026-07-16-local-scheduler-failure-admission` | local | Serialized scheduler failure publication/admission, exact success-masking regression, package consumers, benchmark synthesis, and all scenarios at `979665b` | Pass | 161 Rust tests passed with one AWS-only test ignored; the exact regression passed 100 consecutive runs; 155 Vitest and seven Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all 33 phases across 23 stacks synthesized; both Lambda architectures were rebuilt and package-verified; benchmark synthesis passed. No AWS run was performed because a healthy deployment cannot deterministically inject the required post-failure task ordering. |
| `2026-07-16-aws-copy-retry-ownership` | aws | Direct-copy Create and tamper-repair Update using the arm64 candidate at `4cb0ef5` | Pass and cleaned | Create produced the exact 651-byte ZIP, inferred `application/zip`, and a valid opaque identity. An independent overwrite replaced it with a 1,069-byte text object and removed that identity. A forced Update restored the original bytes byte-for-byte, restored the ZIP type and a valid identity, removed the tamper metadata, and emitted the second clean one-attempt CopyObject diagnostics record. The stack and captured bucket were destroyed and independently confirmed absent. |
| `2026-07-16-local-copy-retry-ownership` | local | CopyObject retry ownership, guards, reconciliation, diagnostics, package consumers, benchmark synthesis, and all scenarios at `4cb0ef5` | Pass | 160 Rust tests passed with one AWS-only test ignored; 155 Vitest and seven Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all 33 phases across 23 stacks synthesized; both Lambda architectures were rebuilt and package-verified; benchmark synthesis passed. |
| `2026-07-16-aws-handler-isolation` | aws | Shared/isolated handler topology and destructive shared-handler replacement using the arm64 candidate at `c93e658` | Pass and cleaned | Two default-sharing deployments and two isolated deployments wrote four exact objects. The deployed template contained three Shin provider functions, generated roles, and policies. A separate 1024-to-2048 MiB replacement chain updated the destination owner before old-generation Delete and retained the exact updated object. Both stacks and both captured buckets were destroyed and confirmed absent. |
| `2026-07-16-local-handler-isolation` | local | Handler identity/isolation, package consumers, benchmark synthesis, and all scenarios at `c93e658` | Pass | 151 Rust tests passed with one AWS-only test ignored; 155 Vitest and seven Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all 33 phases across 23 stacks synthesized; benchmark synthesis passed. |
| `2026-07-16-aws-replacement-owner-generation` | aws | Two ordered handler-memory phases using the arm64 replacement-ownership candidate at `4a8eb6e` | Pass and cleaned | The initial phase wrote the exact initial object. The update changed the bucket ownership tag before creating the replacement, the old custom resource then received cleanup-phase Delete, and the exact updated object remained present afterward. The replacement and isolation stacks and both captured buckets were destroyed and confirmed absent. |
| `2026-07-16-local-replacement-owner-generation` | local | Generation-specific destination ownership, package consumers, benchmark synthesis, and all scenarios at `4a8eb6e` | Pass | 151 Rust tests passed with one AWS-only test ignored; 149 Vitest and seven Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all 32 phases synthesized; benchmark synthesis passed. |
| `2026-07-16-aws-iam-least-privilege` | aws | Two ordered cross-bucket phases using the rebuilt arm64 IAM least-privilege provider at `011ae5f` | Pass and cleaned | The initial phase wrote exact current and obsolete fixtures only to the old prefix. The update removed both old objects through explicit `onChange.fromBucket` authority, wrote the exact new object while current stale deletion was disabled, and left no new obsolete object. The stack and both test buckets were destroyed and confirmed absent. |
| `2026-07-16-local-iam-least-privilege` | local | Exact destination IAM actions, conditional cleanup grants, cross-bucket scenario ordering, package consumers, benchmark synthesis, and all scenarios at `011ae5f` | Pass | 151 Rust tests passed with one AWS-only test ignored; 149 Vitest and seven Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all 32 phases synthesized; benchmark synthesis passed; the arm64 provider was rebuilt for the targeted AWS run. |
| `2026-07-16-aws-lifecycle-namespace-safety` | aws | Six ordered phases across three stacks using the rebuilt arm64 lifecycle namespace-safety provider at `1a3f56b` | Pass and cleaned | A root update preserved another deployment's child object; an unauthorized child-to-parent move retained the obsolete child object while writing current content; explicit `onChange.deleteObjects` removed that obsolete object and preserved the current manifest with `onDeploy.deleteStaleObjects=false`. All three stacks were destroyed and confirmed absent. |
| `2026-07-16-local-lifecycle-namespace-safety` | local | Owner-aware stale cleanup, exact slash handling, child-to-parent lifecycle semantics, reusable scenario apps, package consumers, and all scenarios at `1a3f56b` | Pass | 151 Rust tests passed with one AWS-only test ignored; 146 Vitest and seven Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all 30 phases synthesized; both Lambda architectures were rebuilt and package-verified. |
| `2026-07-15-aws-rust-modules` | aws | 21 phases across 16 stacks using the rebuilt arm64 Rust module-refactor provider at `0b63387` | Pass and cleaned | Every ordered phase and custom resource converged successfully. The run verified integration and CloudFormation success; it did not repeat the component snapshots' independent object-state probes. All scoped stacks and intentionally retained test buckets were removed, and final scoped counts were zero. |
| `2026-07-15-local-rust-modules` | local | Rust module extraction, package consumers, benchmark harness, and all scenarios at `0b63387` | Pass | 135 Rust tests passed with one AWS-only test ignored; 107 Vitest and five Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all scenarios synthesized; both Lambda architectures rebuilt and package-verified. Normalized before/current scenario and benchmark synthesis matched apart from the expected provider asset hash. |
| `2026-07-14-aws-api-docs-evidence` | aws | 21 phases across 16 stacks using the rebuilt arm64 diagnostics-v3 provider at `710e61e`, plus a live simple-scenario telemetry probe | Pass and cleaned | Every ordered phase and custom resource converged successfully. The live probe confirmed schema v3, deployment success, and one confirmed callback response. The run verified integration and CloudFormation success; it did not repeat the component snapshots' independent object-state probes. All scoped stacks and captured test buckets were removed, and final scoped counts were zero. |
| `2026-07-14-local-api-docs-evidence` | local | Public error contract, API documentation, diagnostics-v3 provider, package consumers, benchmark telemetry reader, and scenarios at `710e61e` | Pass | 135 Rust tests passed with one AWS-only test ignored; 107 Vitest and five Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all scenarios synthesized; both Lambda architectures rebuilt and package-verified. |
| `2026-07-14-aws-pr15-rebased` | aws | 21 phases across 16 stacks using the rebuilt arm64 combined provider at `af7873d` | Pass and cleaned | Every ordered phase and custom resource converged successfully, including stale cleanup/retention, destination changes, KMS/DSSE, and CloudFront sync/async paths. The run verified integration and CloudFormation success; it did not repeat the component snapshots' independent object-state probes. All scoped stacks were removed and the final count was zero. |
| `2026-07-14-local-pr15-rebased` | local | Rebased marker streaming, invocation memory planner, conditional stale scan, package, benchmark harness, and scenarios at `af7873d` | Pass | 134 Rust tests passed with one AWS-only test ignored; 90 Vitest and five Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all scenarios synthesized; both Lambda architectures rebuilt and package-verified. |
| `2026-07-14-local-marker-follow-up` | local | Abandoned marker-body cancellation, stable benchmark fixture, package, and scenario candidate at `3967fec` | Pass | 128 Rust tests passed with one AWS-only test ignored; 89 Vitest and five Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all scenarios synthesized; both Lambda architectures rebuilt and package-verified. |
| `2026-07-13-aws-marker-replacement` | aws | 21 phases across 16 stacks using the rebuilt arm64 streaming-marker provider | Pass and cleaned | Every ordered phase completed. Independent assertions covered content/types, all marker source shapes and telemetry, filters, source order, ranged reads, lifecycle state, exact KMS/DSSE checksum shape, and primed sync/async CloudFront invalidation. All stacks and the retained bucket were removed. |
| `2026-07-13-local-marker-replacement` | local | Streaming marker provider, decision-run harness, package, and scenario candidate | Pass | 127 Rust tests passed with one AWS-only test ignored; 87 Vitest and five Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny/workflow/TOML gates passed; all scenarios synthesized; both Lambda architectures rebuilt and package-verified. |
| `2026-07-13-aws-invocation-memory-planning` | aws | 21 phases across 16 stacks using the rebuilt arm64 memory-planning provider | Pass and cleaned | Every ordered phase completed. Fourteen independent state groups verified content/types, filters, marker escaping, source order, lifecycle, ranged reads, exact KMS/DSSE checksums, and primed sync/async CloudFront updates. Eighteen summaries passed the runtime-memory/global-budget/page-bound assertions. All stacks and captured buckets were removed. |
| `2026-07-13-local-invocation-memory-planning` | local | Memory planner, allocator, destination planner, telemetry renderer, package, benchmark harness, and scenario candidate | Pass | 130 Rust tests passed with one AWS-only test ignored; 88 Vitest and five Node tests passed; build/package/typecheck/lint/fmt/clippy/audit/deny gates passed; all scenarios synthesized; both Lambda architectures rebuilt and package-verified. |
| `2026-07-13-aws-transfer-scheduler` | aws | 21 phases across 16 stacks using the rebuilt arm64 transfer-scheduler provider | Pass and cleaned | Every ordered phase completed successfully. The runner destroyed all stacks and a separate scoped check found zero remaining. Raw logs and identifiers remain outside git. |
| `2026-07-13-local-transfer-scheduler` | local | Transfer-scheduler provider, telemetry, benchmark harness, package, and scenario candidate | Pass | 125 Rust tests passed with one AWS-only test ignored; 86 Vitest and five Node tests passed; build/package/typecheck/lint/fmt/clippy gates passed; all scenarios synthesized; both Lambda architectures rebuilt and package-verified. |
| `2026-07-12-local-pr12-release-candidate` | local | Provider source through `7a910f4` and the PR #12 TypeScript/scenario candidate | Pass | 114 Rust tests passed with one AWS-only test ignored; 86 Vitest and five Node tests passed; build/package/typecheck/lint/clippy/package gates passed; all 21 scenarios listed and synthesized; both Lambda architectures rebuilt. |
| `2026-07-12-aws-pr12-release-candidate` | aws | 21 phases across 16 stacks using the rebuilt arm64 provider | Pass and cleaned | Every custom resource reached CloudFormation success. Independent assertions then verified content, inferred types, filters, markers, source order, ranged reads, lifecycle state, exact KMS/DSSE checksums, and sync/async CloudFront invalidation. All stacks and the retained bucket were removed. |
| `2026-07-12-aws-object-semantics` | aws, historical | 21 phases across 15 stacks at `b40def8` | Superseded for current-code status | The run passed and cleaned up, but it exercised the removed metadata semantics, universal SHA-256, ACL reconciliation, and global marker preflight. It is not current PR #12 evidence. |

## Known Limitations

- Marker uploads require one bounded planning pass for exact length and validation, followed by a second bounded source pass when an upload is required; unchanged SSE-S3 objects stop after planning. The separately maintained performance decision run measures and accepts this tradeoff.
- The released AWS S3 dependency chain currently requires newly yanked `spin@0.10.0` through `crc-fast`. The full lockfile audit reports that yanked warning and no vulnerability; the dependency-policy command excludes only that exact transitive version until `crc-fast` publishes a compatible non-yanked dependency.
- The transfer-scheduler decision run contains five before, five current, and four upstream repetitions; the maintainer stopped the final upstream repetition to cap time and accepted the completed performance evidence for PR review. The 112 sanitized phase rows are retained in `benchmarks/results.jsonl`.
- The deployed AWS run exercised arm64. The x86_64 provider was rebuilt from the same source and passed package validation, but was not separately deployed.
- The invocation-global source cap governs both conservative central-directory planning estimates and retained source blocks in non-overlapping phases. Runtime, SDK, transfer-body, manifest, and marker allocations use the other half of Lambda memory and remain bounded by concurrency and object-size rules.
- Destination metadata is O(manifest keys), and stale cleanup is O(one S3 page); the second destination scan runs only when the initial listing identifies an exact included non-manifest candidate. The deployment manifest itself remains proportional to planned object count.
- Lost-response convergence is covered with deterministic S3 wire replay rather than an injected AWS network failure. The live direct-copy run covered ordinary Create and guarded repair after an independent overwrite, but did not inject a lost response. The real KMS/DSSE run confirms that the stored checksum needed by upload reconciliation exists and matches.
- KMS/DSSE destination ETags are not treated as plaintext MD5. Existing encrypted objects can transfer again instead of using the SSE-S3 catalog/ETag shortcut; avoiding a checksum-mode `HeadObject` per destination object keeps the normal deployment request count bounded.
- Deployments remain non-transactional across objects. A marker upload withholds its final body frame until the second pass validates source integrity and planned output identity, but earlier independent object writes may already have completed before a later object fails.
- Imported buckets, SSE-C, and encryption configurations whose strategy cannot be proven at synthesis remain intentionally unsupported.
- Raw AWS evidence remains intentionally excluded from git. Performance results and their separate caveats are maintained in [benchmark](./benchmark.md).
